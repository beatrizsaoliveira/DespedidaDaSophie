// ============================================================
// Global ambient declarations for Despedida da Sophie
//
// Add here:
//  - Ambient module declarations (e.g. non-TS file imports)
//  - Window / globalThis augmentations
//  - Build-time constant injections (e.g. from esbuild define)
// ============================================================

// Uncomment if esbuild ever injects a build-time version:
// declare const __APP_VERSION__: string;

// Static asset imports (if esbuild asset loader is configured):
// declare module '*.jpg' { const src: string; export default src; }
// declare module '*.png' { const src: string; export default src; }
// declare module '*.svg' { const src: string; export default src; }
